# Copy to supabase/.env.prod (gitignored) and fill in.
# Dashboard -> project -> Connect -> "Session pooler" (IPv4-safe). Include the password inline.
# Keep this password DIFFERENT from the dev one, and never paste it into a dev-facing file.
SUPABASE_DB_URL=postgresql://postgres.wiuuouqkpfwpyudayagc:YOUR_PROD_DB_PASSWORD@aws-0-us-east-2.pooler.supabase.com:5432/postgres
