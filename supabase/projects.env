# Forked Supabase projects — the single source of truth for which ref is which.
# Project refs are NOT secrets (they are in every client URL), so this file is committed.
# Passwords / connection strings live in supabase/.env.dev and supabase/.env.prod (gitignored).
#
# Change the mapping HERE and every script + npm alias follows. Never hardcode a ref elsewhere.

SUPABASE_PROJECT_REF_DEV=bqxhinoabxmpsvzntrlq
SUPABASE_PROJECT_NAME_DEV="Forked (dev)"
SUPABASE_PROJECT_REGION_DEV=us-west-2

SUPABASE_PROJECT_REF_PROD=wiuuouqkpfwpyudayagc
SUPABASE_PROJECT_NAME_PROD="Forked-prod"
SUPABASE_PROJECT_REGION_PROD=us-east-2
