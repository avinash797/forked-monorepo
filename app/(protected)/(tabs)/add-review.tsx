import { View } from 'react-native';

// This screen is not displayed - the tab button redirects to the rating flow
// The file exists only to satisfy Expo Router's routing requirements
export default function AddReviewScreen() {
    return <View />;
}
